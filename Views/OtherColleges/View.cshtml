@model IEnumerable<UAAAS.Models.OtherCollege>
@{
    ViewBag.Title = "Other Colleges and Courses";
    Layout = "~/Views/Shared/_LayoutLeftMenu.cshtml";
    int num = 1;
    int cNum = 1;
}


@*<p class="create_link_right">
    <input name="btnPrint" type="button" onclick="printdiv('div_print');" value=" Print" class="create_button">
</p>*@
<script type="text/javascript">
    $(document).ready(function () {

        var mainUrl = window.location;
        var url;
        $(".view_hide").show();
        $.ajaxSetup({ cache: false });

        var $myDialog = $("#dialog-edit").dialog({
            title: 'Add/Edit College',
            autoOpen: false,
            resizable: false,
            height: 350,
            width: 600,
            show: { effect: 'drop', direction: "up" },
            modal: true,
            draggable: true,
            closeOnEscape: true,
            open: function (event, ui) {
                $(this).load(url);
            },
            close: function (event, ui) {
                $(this).dialog("close");
            }
        });

        $("#openDialog").live("click", function (e) {
            e.preventDefault();
            $("#dialog-edit").dialog({
                title: 'Add College'
            });
            url = $("#openDialog").attr('href');

            $myDialog.dialog('open');
            return false;
        });

        $('.full_table').find('tr').click(function () {
            url = $(this).find(".editDialog").attr('href');
        });

        $(".editDialog").live("click", function (e) {
            e.preventDefault();
            $("#dialog-edit").dialog({
                title: 'Edit College'
            });
            //url = $(".editDialog").attr('href');
            $myDialog.dialog('open');
            return false;
        });

        $(".confirmDialog").live("click", function (e) {
            var url = $(this).attr('href');
            $("#dialog-confirm").dialog({
                title: 'Delete College',
                autoOpen: false,
                resizable: false,
                height: 170,
                width: 350,
                show: { effect: 'drop', direction: "up" },
                modal: true,
                draggable: true,
                buttons: {
                    "Yes": function () {
                        $(this).dialog("close");
                        window.location = url;

                    },
                    "No": function () {
                        $(this).dialog("close");

                    }
                }
            });
            $("#dialog-confirm").dialog('open');
            return false;
        });

        $(".viewDialog").live("click", function (e) {
            var url = $(this).attr('href');
            $("#dialog-view").dialog({
                title: 'View College',
                autoOpen: false,
                resizable: false,
                height: 355,
                width: 600,
                show: { effect: 'drop', direction: "up" },
                modal: true,
                draggable: true,
                open: function (event, ui) {
                    $(this).load(url);

                },
                buttons: {
                    "Close": function () {
                        $(this).dialog("close");

                    }
                },
                close: function (event, ui) {
                    $(this).dialog('close');
                }
            });

            $("#dialog-view").dialog('open');
            return false;
        });

        $("#btncancel").live("click", function (e) {
            $("#dialog-edit").dialog("close");
        });

        $($myDialog).bind("dialogclose", function () {
            window.location = mainUrl;
        });
    });
</script>

<script type="text/javascript">
    $(document).ready(function () {
        $(".view_hide").show();
        var mainUrl = window.location;
        var courseurl;

        $.ajaxSetup({ cache: false });

        var $myCourseDialog = $("#dialog-edit-course").dialog({
            title: 'Add/Edit Course',
            autoOpen: false,
            resizable: false,
            height: 350,
            width: 600,
            show: { effect: 'drop', direction: "up" },
            modal: true,
            draggable: true,
            closeOnEscape: true,
            open: function (event, ui) {
                $(this).load(courseurl);
            },
            close: function (event, ui) {
                $(this).dialog("close");
            }
        });

        $("#openDialogCourse").live("click", function (e) {
            e.preventDefault();
            $("#dialog-edit-course").dialog({
                title: 'Add Course'
            });
            courseurl = $("#openDialogCourse").attr('href');

            $myCourseDialog.dialog('open');
            return false;
        });

        $('.full_table_course').find('tr').click(function () {

            courseurl = $(this).find(".editDialogCourse").attr('href');

        });

        $(".editDialogCourse").live("click", function (e) {
            e.preventDefault();
            $("#dialog-edit-course").dialog({
                title: 'Edit Course'
            });
            //url = $(".editDialog").attr('href');
            $myCourseDialog.dialog('open');
            return false;
        });

        $(".confirmDialogCourse").live("click", function (e) {

            var courseurl = $(this).attr('href');
            $("#dialog-confirm-course").dialog({
                title: 'Delete Course',
                autoOpen: false,
                resizable: false,
                height: 170,
                width: 350,
                show: { effect: 'drop', direction: "up" },
                modal: true,
                draggable: true,
                buttons: {
                    "Yes": function () {
                        $(this).dialog("close");
                        window.location = courseurl;

                    },
                    "No": function () {
                        $(this).dialog("close");

                    }
                }
            });
            $("#dialog-confirm-course").dialog('open');
            return false;
        });

        $(".viewDialogCourse").live("click", function (e) {
            var courseurl = $(this).attr('href');
            $("#dialog-view-course").dialog({
                title: 'View Course',
                autoOpen: false,
                resizable: false,
                height: 355,
                width: 400,
                show: { effect: 'drop', direction: "up" },
                modal: true,
                draggable: true,
                open: function (event, ui) {
                    $(this).load(courseurl);
                },
                buttons: {
                    "Close": function () {
                        $(this).dialog("close");

                    }
                },
                close: function (event, ui) {
                    $(this).dialog('close');
                }
            });

            $("#dialog-view-course").dialog('open');
            return false;
        });

        $("#btncancelCourse").live("click", function (e) {
            $("#dialog-edit-course").dialog("close");
        });

        $($myCourseDialog).bind("dialogclose", function () {
            window.location = mainUrl;
        });
    });

    function printdiv(printpage) {
        $(".view_hide").hide();
        //Get the HTML of div
        var divElements = document.getElementById(printpage).innerHTML;
        //Get the HTML of whole page
        var oldPage = document.body.innerHTML;

        //Reset the page's HTML with div's HTML only
        document.body.innerHTML =
          "<html><head><title></title></head><body>" +
          divElements.fontsize(1) + "</body>";

        //Print Page
        window.print();

        //Restore orignal HTML
        document.body.innerHTML = oldPage;
        $(".view_hide").show();
        return false;
    }
</script>

<script type="text/javascript">
    $(document).ready(function () {

        var mainUrl = window.location;
        var url;

        $.ajaxSetup({ cache: false });

        var $myDialogSameSocity = $("#dialog-editSameSocity").dialog({
            title: 'Add/Edit Other College Same Socity',
            autoOpen: false,
            resizable: false,
            height: 350,
            width: 600,
            show: { effect: 'drop', direction: "up" },
            modal: true,
            draggable: true,
            closeOnEscape: true,
            open: function (event, ui) {
                $(this).load(url);
            },
            close: function (event, ui) {
                //$(this).dialog("close");
            }
        });

        $("#openDialogSameSocity").live("click", function (e) {
            e.preventDefault();
            $("#dialog-editSameSocity").dialog({
                title: 'Add Other College Same Socity'
            });
            url = $("#openDialogSameSocity").attr('href');

            $myDialogSameSocity.dialog('open');
            return false;
        });

        $('.full_table').find('tr').click(function () {
            url = $(this).find(".editDialogSameSocity").attr('href');
        });

        $(".editDialogSameSocity").live("click", function (e) {
            e.preventDefault();
            $("#dialog-edit").dialog({
                title: 'Edit Other College Same Socity'
            });
            //url = $(".editDialogSameSocity").attr('href');
            $myDialogSameSocity.dialog('open');
            return false;
        });

        $(".confirmDialogSameSocity").live("click", function (e) {
            var url = $(this).attr('href');
            $("#dialog-confirmSameSocity").dialog({
                title: 'Delete Other College Same Socity',
                autoOpen: false,
                resizable: false,
                height: 170,
                width: 350,
                show: { effect: 'drop', direction: "up" },
                modal: true,
                draggable: true,
                buttons: {
                    "Yes": function () {
                        $(this).dialog("close");
                        window.location = url;

                    },
                    "No": function () {
                        $(this).dialog("close");

                    }
                }
            });
            $("#dialog-confirmSameSocity").dialog('open');
            return false;
        });

        $(".viewDialogSameSocity").live("click", function (e) {
            var url = $(this).attr('href');
            $("#dialog-viewSameSocity").dialog({
                title: 'View Other College Same Socity',
                autoOpen: false,
                resizable: false,
                height: 350,
                width: 600,
                show: { effect: 'drop', direction: "up" },
                modal: true,
                draggable: true,
                open: function (event, ui) {
                    $(this).load(url);

                },
                buttons: {
                    "Close": function () {
                        $(this).dialog("close");

                    }
                },
                close: function (event, ui) {
                    $(this).dialog('close');
                }
            });

            $("#dialog-viewSameSocity").dialog('open');
            return false;
        });

        $("#btncancelSameSocity").live("click", function (e) {
            $("#dialog-editSameSocity").dialog("close");
        });

        $($myDialogSameSocity).bind("dialogclose", function () {
            window.location = mainUrl;
        });
    });
</script>

<div id="div_print">
   <h2>Other Colleges and Courses</h2>
        <table class="four_columns" style="width: 100%;">
            <tr>
                <td colspan="5" class="col11">
                    <b>Other Colleges in the same premises</b>
                </td>

            </tr>
            <tr>
                <th style="text-align: left;" class="col11">S.No
                </th>
                <th style="text-align: left;" class="col11">College Name
                </th>
                <th style="text-align: left;" class="col11">Year Of Establishment(YYYY)
                </th>
                <th style="text-align: left;" class="col11">Affiliated University
                </th>
                <th colspan="3" style="text-align: center;" class="col11 view_hide"></th>
            </tr>

            @foreach (var item in ViewBag.Colleges as List<UAAAS.Models.OtherCollege>)
            {
                <tr>
                    <td style="text-align: left;" class="col21">
                        @(num++)
                    </td>
                    <td style="text-align: left;" class="col21">
                        @Html.DisplayFor(modelItem => item.collegeName)
                    </td>
                    <td style="text-align: left;" class="col21">
                        @Html.DisplayFor(modelItem => item.yearOfEstablishment)
                    </td>
                    <td style="text-align: left;" class="col21">
                        @if (!string.IsNullOrWhiteSpace(item.otherUniversityName))
                        {
                            @Html.DisplayFor(modelItem => item.otherUniversityName)
                        }
                        else
                        {
                            @Html.DisplayFor(modelItem => item.universityName)
                        }
                    </td>

                    <td style="text-align: left;" class="col21 view_hide">
                        @Html.ActionLink("Details", "Details", new { id = item.id }, new { @class = "viewDialog view_button" })
                    </td>

                </tr>
            }

        </table>
        @if (ViewBag.CollegeCount == 0)
        {
        <div class="warning" style="width: 200px; margin: 0 auto;">
            <p>No Data found</p>
        </div>
        }

        <div id="dialog-view" style="display: none">
        </div>

        <br />
        <br />

        <table class="four_columns" style="width: 100%;">
            <tr>
                <td colspan="4" class="col11">
                    <b>Other Courses in the same premises</b>
                </td>

            </tr>
            <tr>
                <th style="text-align: left;" class="col11">S.No
                </th>
                <th style="text-align: left;" class="col11">Course Name 
                </th>
                <th style="text-align: left;" class="col11">Affiliated University
                </th>
                <th colspan="3" style="text-align: center;" class="col11  view_hide"></th>
            </tr>

            @foreach (var item in ViewBag.Courses as List<UAAAS.Models.OtherCourse>)
            {
                <tr>
                    <td style="text-align: left;" class="col21">
                        @(cNum++)
                    </td>
                    <td style="text-align: left;" class="col21">
                        @Html.DisplayFor(modelItem => item.courseName)
                    </td>
                    <td style="text-align: left;" class="col21">
                        @if (!string.IsNullOrWhiteSpace(item.otherUniversityName))
                        {
                            @Html.DisplayFor(modelItem => item.otherUniversityName)
                        }
                        else
                        {
                            @Html.DisplayFor(modelItem => item.universityName)
                        }
                    </td>

                    <td style="text-align: left;" class="col21 view_hide">
                        @Html.ActionLink("Details", "Details", "OtherCourses", new { id = item.id }, new { @class = "viewDialogCourse view_button" })
                    </td>

                </tr>
            }

        </table>
        @if (ViewBag.CourseCount == 0)
        {
        <div class="warning" style="width: 200px; margin: 0 auto;">
            <p>No Data found</p>
        </div>
        }
    
        <div id="dialog-view-course" style="display: none">
        </div>

    <br />
    <br />

     <table class="four_columns" style="width: 100%;">
            <tr>
                <td colspan="5" class="col11">
                    <b>Other Colleges under the same socity</b>
                </td>

            </tr>
            <tr>
                <th style="text-align: left;" class="col11">S.No
                </th>
                <th style="text-align: left;" class="col11">College Name & Address
                </th>
                <th style="text-align: left;" class="col11">Year Of Establishment(YYYY)
                </th>
                <th style="text-align: left;" class="col11">College Location
                </th>
                <th style="text-align: left;" class="col11">Affiliated University
                </th>
              @*  <th colspan="3" style="text-align: center;" class="col11 view_hide"></th>*@
            </tr>

            @foreach (var item in ViewBag.otherCollegesSameSociety as List<UAAAS.Models.OtherCollegesSameSociety>)
            {
                <tr>
                    <td style="text-align: left;" class="col21">
                        @(num++)
                    </td>
                    <td style="text-align: left;" class="col21">
                        @Html.DisplayFor(modelItem => item.collegeName)
                    </td>
                    <td style="text-align: left;" class="col21">
                        @Html.DisplayFor(modelItem => item.yearOfEstablishment)
                    </td>
                    <td style="text-align: left;" class="col21">
                        @Html.DisplayFor(modelItem => item.collegeLocation)
                    </td>
                    <td style="text-align: left;" class="col21">
                        @if (!string.IsNullOrWhiteSpace(item.otherUniversityName))
                        {
                            @Html.DisplayFor(modelItem => item.otherUniversityName)
                        }
                        else
                        {
                            @Html.DisplayFor(modelItem => item.universityName)
                        }
                    </td>

                @*    <td style="text-align: left;" class="col21 view_hide">
                        @Html.ActionLink("Details", "Details","OtherCollegesSameSociety", new { id = item.id }, new { @class = "viewDialogSameSocity view_button" })
                    </td>*@

                </tr>
            }

        </table>
        @if (ViewBag.otherCollegesSameSocietyCount == 0)
        {
        <div class="warning" style="width: 200px; margin: 0 auto;">
            <p>No Data found</p>
        </div>
        }

        <div id="dialog-viewSameSocity" style="display: none">
        </div>

    
</div>
