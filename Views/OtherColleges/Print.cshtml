@*@model IEnumerable<UAAAS.Models.OtherCollege>
*@
@model Tuple<IEnumerable<UAAAS.Models.OtherCollege>, IEnumerable<UAAAS.Models.OtherCourse>>
@{   
    int num = 1;
    int cNum = 1;
}


<script type="text/javascript">
    $(document).ready(function () {

        var mainUrl = window.location;
        var url;

        $.ajaxSetup({ cache: false });

        var $myDialog = $("#dialog-edit").dialog({
            title: 'Add/Edit College',
            autoOpen: false,
            resizable: false,
            height: 350,
            width: 600,
            show: { effect: 'drop', direction: "up" },
            modal: true,
            draggable: true,
            closeOnEscape: true,
            open: function (event, ui) {
                $(this).load(url);
            },
            close: function (event, ui) {
                $(this).dialog("close");
            }
        });

        $("#openDialog").live("click", function (e) {
            e.preventDefault();
            $("#dialog-edit").dialog({
                title: 'Add College'
            });
            url = $("#openDialog").attr('href');

            $myDialog.dialog('open');
            return false;
        });

        $('.full_table').find('tr').click(function () {
            url = $(this).find(".editDialog").attr('href');
        });

        $(".editDialog").live("click", function (e) {
            e.preventDefault();
            $("#dialog-edit").dialog({
                title: 'Edit College'
            });
            //url = $(".editDialog").attr('href');
            $myDialog.dialog('open');
            return false;
        });

        $(".confirmDialog").live("click", function (e) {
            var url = $(this).attr('href');
            $("#dialog-confirm").dialog({
                title: 'Delete College',
                autoOpen: false,
                resizable: false,
                height: 170,
                width: 350,
                show: { effect: 'drop', direction: "up" },
                modal: true,
                draggable: true,
                buttons: {
                    "Yes": function () {
                        $(this).dialog("close");
                        window.location = url;

                    },
                    "No": function () {
                        $(this).dialog("close");

                    }
                }
            });
            $("#dialog-confirm").dialog('open');
            return false;
        });

        $(".viewDialog").live("click", function (e) {
            var url = $(this).attr('href');
            $("#dialog-view").dialog({
                title: 'View College',
                autoOpen: false,
                resizable: false,
                height: 355,
                width: 400,
                show: { effect: 'drop', direction: "up" },
                modal: true,
                draggable: true,
                open: function (event, ui) {
                    $(this).load(url);

                },
                buttons: {
                    "Close": function () {
                        $(this).dialog("close");

                    }
                },
                close: function (event, ui) {
                    $(this).dialog('close');
                }
            });

            $("#dialog-view").dialog('open');
            return false;
        });

        $("#btncancel").live("click", function (e) {
            $("#dialog-edit").dialog("close");
        });

        $($myDialog).bind("dialogclose", function () {
            window.location = mainUrl;
        });
    });
</script>
<table style="width: 100%" border="1">
    <tr>
        <td>
            <b>Other Colleges in the same premises</b>
        </td>

    </tr>
</table>
<table class="full_table" style="width: 100%;" border="1">
    <tr>
        <th style="text-align: center;">S.No
        </th>
        <th>College Name
        </th>
        <th style="text-align: center;">Affiliated University
        </th>
        <th colspan="3" style="text-align: center;"></th>
    </tr>

    @foreach (var item in Model.Item1)
    {
        <tr>
            <td style="width: 50px; text-align: center;">
                @(num++)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.collegeName)
            </td>
            <td style="width: 200px; text-align: center;">
                @if (!string.IsNullOrWhiteSpace(item.otherUniversityName))
                {
                    @Html.DisplayFor(modelItem => item.otherUniversityName)
                }
                else
                {
                    @Html.DisplayFor(modelItem => item.universityName)
                }
            </td>
        </tr>
    }

</table>
<div id="dialog-view" style="display: none">
</div>

<br />
<br />
<script type="text/javascript">
    $(document).ready(function () {

        var mainUrl = window.location;
        var courseurl;

        $.ajaxSetup({ cache: false });

        var $myCourseDialog = $("#dialog-edit-course").dialog({
            title: 'Add/Edit Course',
            autoOpen: false,
            resizable: false,
            height: 350,
            width: 600,
            show: { effect: 'drop', direction: "up" },
            modal: true,
            draggable: true,
            closeOnEscape: true,
            open: function (event, ui) {
                $(this).load(courseurl);
            },
            close: function (event, ui) {
                $(this).dialog("close");
            }
        });

        $("#openDialogCourse").live("click", function (e) {
            e.preventDefault();
            $("#dialog-edit-course").dialog({
                title: 'Add Course'
            });
            courseurl = $("#openDialogCourse").attr('href');

            $myCourseDialog.dialog('open');
            return false;
        });

        $('.full_table_course').find('tr').click(function () {

            courseurl = $(this).find(".editDialogCourse").attr('href');

        });

        $(".editDialogCourse").live("click", function (e) {
            e.preventDefault();
            $("#dialog-edit-course").dialog({
                title: 'Edit Course'
            });
            //url = $(".editDialog").attr('href');
            $myCourseDialog.dialog('open');
            return false;
        });

        $(".confirmDialogCourse").live("click", function (e) {

            var courseurl = $(this).attr('href');
            $("#dialog-confirm-course").dialog({
                title: 'Delete Course',
                autoOpen: false,
                resizable: false,
                height: 170,
                width: 350,
                show: { effect: 'drop', direction: "up" },
                modal: true,
                draggable: true,
                buttons: {
                    "Yes": function () {
                        $(this).dialog("close");
                        window.location = courseurl;

                    },
                    "No": function () {
                        $(this).dialog("close");

                    }
                }
            });
            $("#dialog-confirm-course").dialog('open');
            return false;
        });

        $(".viewDialogCourse").live("click", function (e) {
            var courseurl = $(this).attr('href');
            $("#dialog-view-course").dialog({
                title: 'View Course',
                autoOpen: false,
                resizable: false,
                height: 355,
                width: 400,
                show: { effect: 'drop', direction: "up" },
                modal: true,
                draggable: true,
                open: function (event, ui) {
                    $(this).load(courseurl);
                },
                buttons: {
                    "Close": function () {
                        $(this).dialog("close");

                    }
                },
                close: function (event, ui) {
                    $(this).dialog('close');
                }
            });

            $("#dialog-view-course").dialog('open');
            return false;
        });

        $("#btncancelCourse").live("click", function (e) {
            $("#dialog-edit-course").dialog("close");
        });

        $($myCourseDialog).bind("dialogclose", function () {
            window.location = mainUrl;
        });
    });
</script>
<table style="width: 100%" border="1">
    <tr>
        <td>
            <b>Other Courses in the same premises</b>
        </td>

    </tr>
</table>
<table class="full_table_course" style="width: 100%;" border="1">
    <tr>
        <th style="text-align: center;">S.No
        </th>
        <th>College Name
        </th>
        <th style="text-align: center;">Affiliated University
        </th>
        <th colspan="3" style="text-align: center;"></th>
    </tr>

    @foreach (var item in Model.Item2)
    {
        <tr>
            <td style="width: 50px; text-align: center;">
                @(cNum++)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.courseName)
            </td>
            <td style="width: 200px; text-align: center;">
                @if (!string.IsNullOrWhiteSpace(item.otherUniversityName))
                {
                    @Html.DisplayFor(modelItem => item.otherUniversityName)
                }
                else
                {
                    @Html.DisplayFor(modelItem => item.universityName)
                }
            </td>
        </tr>
    }

</table>
<div id="dialog-view-course" style="display: none">
</div>


@Html.ActionLink("Print", "Print", null, new { target = "_blank" })