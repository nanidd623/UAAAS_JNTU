@model IEnumerable<UAAAS.Models.jntuh_college_pgcourses>
@{
    Layout = "~/Views/Shared/_LayoutLeftMenu.cshtml";
    if (Roles.IsUserInRole("Admin") || Roles.IsUserInRole("DataEntry"))
    {
        Layout = "~/Views/Shared/_DataEntryLayoutLeftMenu.cshtml";
    }
    ViewBag.Title = "PG Courses";
    int num = 1;
}

<script type="text/javascript">
    $(document).ready(function () {

        var mainUrl = window.location;
        var url;

        $.ajaxSetup({ cache: false });

        var $myDialog = $("#dialog-edit").dialog({
            title: 'Add/Edit PG Course',
            autoOpen: false,
            resizable: false,
            height: 400,
            width: 500,
            show: { effect: 'drop', direction: "up" },
            modal: true,
            draggable: true,
            closeOnEscape: true,
            open: function (event, ui) {
                $(this).load(url);
            },
            close: function (event, ui) {
                $(this).dialog("close");
            }
        });

        $("#openDialog").live("click", function (e) {
            e.preventDefault();
            $("#dialog-edit").dialog({
                title: 'Add PG Course'
            });
            url = $("#openDialog").attr('href');

            $myDialog.dialog('open');
            return false;
        });

        $('.full_table').find('tr').click(function () {
            url = $(this).find(".editDialog").attr('href');
        });

        $(".editDialog").live("click", function (e) {
            e.preventDefault();
            $("#dialog-edit").dialog({
                title: 'Edit PG Course'
            });

            $myDialog.dialog('open');
            return false;
        });

        $(".confirmDialog").live("click", function (e) {
            var url = $(this).attr('href');
            $("#dialog-confirm").dialog({
                title: 'Delete PG Course',
                autoOpen: false,
                resizable: false,
                height: 170,
                width: 350,
                show: { effect: 'drop', direction: "up" },
                modal: true,
                draggable: true,
                buttons: {
                    "Yes": function () {
                        $(this).dialog("close");
                        window.location = url;

                    },
                    "No": function () {
                        $(this).dialog("close");

                    }
                }
            });
            $("#dialog-confirm").dialog('open');
            return false;
        });

        $(".viewDialog").live("click", function (e) {
            var url = $(this).attr('href');
            $("#dialog-view").dialog({
                title: 'View PG Course',
                autoOpen: false,
                resizable: false,
                height: 600,
                width: 650,
                show: { effect: 'drop', direction: "up" },
                modal: true,
                draggable: true,
                open: function (event, ui) {
                    $(this).load(url);
                },
                buttons: {
                    "Close": function () {
                        $(this).dialog("close");
                    }
                },
                close: function (event, ui) {
                    $(this).dialog('close');
                }
            });

            $("#dialog-view").dialog('open');
            return false;
        });

        $("#btncancel").live("click", function (e) {
            $("#dialog-edit").dialog("close");
        });

        $($myDialog).bind("dialogclose", function () {
            window.location = mainUrl;
        });
    });
</script>

@if (ViewBag.NotUpload == true)
{
    <span class="notUpload">You can not upload data Please contact Affiliations & Academic Audit if you want to upload</span>
}
else
{
    if (TempData["Error"] != null)
    {
    <div class="error">
        <p><strong>Error:</strong> @TempData["Error"].ToString()</p>
    </div>
    }
    if (TempData["CollegesSuccess"] != null)
    {
    <div class="success">
        <p><strong>Success:</strong> @TempData["CollegesSuccess"].ToString()</p>
    </div>
    }
    
    <div style="width: 300px; float: left;">
        <h2>PG Courses</h2>
    </div>
    
   // if (ViewBag.IsEditable == false)
    //{
   // <span class="notUpload" style="color: red;">You can't upload data, Please contact Affiliations & Academic Audit if you want to upload / modify</span>    
   // }

    //if (ViewBag.IsEditable == true)
    //{
    <div style="width: 400px; float: right; text-align: right;">
        <table style="width: 100%; margin-bottom: 10px;">
            <tr>
                <td class="create_link_right" style="width: 200px;">
                    @Html.ActionLink("Add PG Course", "AddEditRecord", "CollegePGCourse", new { collegeId = @ViewBag.collegeId }, new { @id = "openDialog1", @class = "create_button" })
                </td>
            </tr>
        </table>
    </div>
  //  }
    
    <div style="clear: both;"></div>
    <table class="full_table" style="width: 100%;">
        <tr>
            <th style="text-align: center;">S.No
            </th>
            <th style="text-align: left;">Department
            </th>
            <th style="text-align: left;">Name of the Programme
            </th>
            <th style="text-align: center;">Programme Type
            </th>
            <th style="text-align: center;">Shift
            </th>
            <th style="text-align: center;">Intake
            </th>
            <th style="text-align: center;">Professors
            </th>
            <th style="text-align: center;">Associate Professors
            </th>
            <th style="text-align: center;">Assistant Professors
            </th>
            <th style="text-align: center;">UG Faculty Student Ratio
            </th>
            <th colspan="2" style="text-align: center;"></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td style="width: 30px; text-align: center; vertical-align: top;">
                    @(num++)
                </td>
                <td style="width: 150px; text-align: left; vertical-align: top;">@item.jntuh_department.departmentName</td>
                <td style="text-align: left; vertical-align: top;">@item.jntuh_specialization.specializationName</td>
                <td style="width: 40px; text-align: center; vertical-align: top;">@item.Type</td>
                <td style="width: 40px; text-align: center; vertical-align: top;">@item.jntuh_shift.shiftName</td>
                <td style="width: 40px; text-align: center; vertical-align: top;">@item.intake</td>
                <td style="width: 70px; text-align: center; vertical-align: top;">@item.professors</td>
                <td style="width: 70px; text-align: center; vertical-align: top;">@item.associateProfessors</td>
                <td style="width: 70px; text-align: center; vertical-align: top;">@item.assistantProfessors</td>
                <td style="width: 70px; text-align: center; vertical-align: top;">@item.UGFacultyStudentRatio</td>
                @*<td style="width: 20px; text-align: center; vertical-align: top;">
                    @Html.ActionLink("Edit", "AddEditRecord", new { id = item.id }, new { @class = "editDialog1 edit_button" })
                </td>*@
                <td style="width: 20px; text-align: center; vertical-align: top;">
                    @*   "Details/" + @ViewContext.RouteData.Values["id"] don't change This is for Admin and College purpose*@
                    @Html.ActionLink("View/Print", "Details/" + @ViewContext.RouteData.Values["id"], "CollegePGCourse", new { cpgid = item.id,collegeId=@ViewBag.collegeId}, new { @class = "details_button", style = "color: red;" })
                </td>
                <td style="width: 20px; text-align: center; vertical-align: top;">
                  @*  @if (ViewBag.IsEditable == true)
                    {*@
                        @Html.ActionLink("Delete", "Delete", new { id = item.id }, new { @class = "confirmDialog delete_button" })

                  @* // }*@
                </td>
            </tr>
        }

    </table>
        if (ViewBag.Count == 0)
        {
    <div class="warning" style="width: 200px; margin: 0 auto;">
        <p>No records found</p>
    </div>
        }
    <div id="dialog-confirm" style="display: none">
        <p>
            <span class="ui-icon ui-icon-alert" style="float: left; margin: 0 7px 20px 0;"></span>
            Are you sure to delete ?
        </p>
    </div>
    <div id="dialog-edit" style="display: none">
    </div>
    <div id="dialog-view" style="display: none">
    </div>
}