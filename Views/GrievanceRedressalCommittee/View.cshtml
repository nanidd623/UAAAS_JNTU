@model UAAAS.Models.GrievanceRedressalCommittee
@{
    ViewBag.Title = "Grievance Redressal Committee";
    Layout = "~/Views/Shared/_LayoutLeftMenu.cshtml";
}

<script type="text/javascript">
    $(document).ready(function () {
        $(".view_hide").show();
        var mainUrl = window.location;
        var url;

        $.ajaxSetup({ cache: false });

        var $myDialog = $("#dialog-edit").dialog({
            title: 'Add/Edit Grievance Redressal Committee',
            autoOpen: false,
            resizable: false,
            height: 200,
            width: 450,
            show: { effect: 'drop', direction: "up" },
            modal: true,
            draggable: true,
            closeOnEscape: true,
            open: function (event, ui) {
                $(this).load(url);
            },
            close: function (event, ui) {
                $(this).dialog("close");
            }
        });

        $("#openDialog").live("click", function (e) {
            e.preventDefault();
            $("#dialog-edit").dialog({
                title: 'Add Grievance Redressal Committee'
            });
            url = $("#openDialog").attr('href');

            $myDialog.dialog('open');
            return false;
        });

        //$('.full_table').find('tr').click(function () {
        $('.webGrid').find('tr').click(function () {
            url = $(this).find(".editDialog").attr('href');
        });

        $(".editDialog").live("click", function (e) {
            e.preventDefault();
            $("#dialog-edit").dialog({
                title: 'Edit Grievance Redressal Committee'
            });
            $myDialog.dialog('open');
            return false;
        });

        $(".confirmDialog").live("click", function (e) {

            var url = $(this).attr('href');
            $("#dialog-confirm").dialog({
                title: 'Delete Grievance Redressal Committee',
                autoOpen: false,
                resizable: false,
                height: 170,
                width: 350,
                show: { effect: 'drop', direction: "up" },
                modal: true,
                draggable: true,
                buttons: {
                    "Yes": function () {
                        $(this).dialog("close");
                        window.location = url;

                    },
                    "No": function () {
                        $(this).dialog("close");

                    }
                }
            });
            $("#dialog-confirm").dialog('open');
            return false;
        });

        $(".viewDialog").live("click", function (e) {
            var url = $(this).attr('href');
            $("#dialog-view").dialog({
                title: 'View Grievance Redressal Committee',
                autoOpen: false,
                resizable: false,
                height: 350,
                width: 500,
                show: { effect: 'drop', direction: "up" },
                modal: true,
                draggable: true,
                open: function (event, ui) {
                    $(this).load(url);

                },
                buttons: {
                    "Close": function () {
                        $(this).dialog("close");

                    }
                },
                close: function (event, ui) {
                    $(this).dialog('close');
                }
            });

            $("#dialog-view").dialog('open');
            return false;
        });

        $("#btncancel").live("click", function (e) {
            $("#dialog-edit").dialog("close");
        });

        $($myDialog).bind("dialogclose", function () {
            window.location = mainUrl;
        });
    });

    function printdiv(printpage) {
        $(".view_hide").hide();
        //Get the HTML of div
        var divElements = document.getElementById(printpage).innerHTML;
        //Get the HTML of whole page
        var oldPage = document.body.innerHTML;

        //Reset the page's HTML with div's HTML only
        document.body.innerHTML =
          "<html><head><title></title></head><body>" +
          divElements.fontsize(1) + "</body>";

        //Print Page
        window.print();

        //Restore orignal HTML
        document.body.innerHTML = oldPage;
        $(".view_hide").show();
        return false;
    }
</script>

<div id="div_print">
    <h2>Grievance Redressal Committee</h2>
    @if (!string.IsNullOrEmpty(ViewBag.collegeGRCpath))
    {
        <div style="float:right;">
            <span><a href="../Content/GRC_Regulations.pdf" class="viewFileData" target="_blank"><span style="font-weight: bold; color: red">GRC Constitution  <img src="../Content/images/latest.gif" alt="latest" style="vertical-align: middle" /></span></a></span>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;
            <span><a href="../@ViewBag.collegeGRCpath" class="viewFileData" target="_blank"><span style="font-weight: bold; color: red">GRC-University Nominee  <img src="../Content/images/latest.gif" alt="latest" style="vertical-align: middle" /></span></a></span>
        </div>
    }
    @using (Html.BeginForm())
    {
        int count = 0;

        <table class="webGrid" style="width: 100%;">
            <tr>
                <td colspan="2" class="col1">
                    <span>Proceedings of the Grievance Redressal Committee </span>@*for the A.Y. @ViewBag.PreviousYear:*@
                </td>
            </tr>
            <tr>
                <td class="col2" colspan="2">
                    @if (!string.IsNullOrEmpty(ViewBag.SupportDocument))
                    {
                        <span><a href="../Content/Upload/CollegeEnclosures/GrievanceRedressal/@ViewBag.SupportDocument" class="viewFileData" target="_blank"><span style="font-weight: bold; color: red">View</span></a></span>


                    }
                </td>
            </tr>
        </table>

        <br />
        <table class="four_columns" style="width: 100%;">
            <tr>
                <td class="col11" colspan="7">
                    <b>Grievance Redressal Committee:</b>
                </td>
            </tr>
            <tr style="font-weight: bold;">
                <td style="text-align: center; width: 50px;" class="col11">S.No</td>
                <td style="text-align: left; width: 250px;" class="col11">Actual Designation</td>
                <td style="text-align: left; width: 250px;" class="col11">Designation in the committee</td>
                <td style="text-align: left;" class="col11">Member Name</td>
                <td style="text-align: left;" class="col11">Email</td>
                <td style="text-align: left;" class="col11">Mobile</td>
                <td style="text-align: left;" class="col11 view_hide"></td>
            </tr>

            @foreach (var item in ViewBag.Committee)
            {
                <tr>
                    <td style="text-align: center;" class="col21">
                        @(count = count + 1)
                    </td>
                    <td style="text-align: left;" class="col21">
                        @if (item.actualDesignation != null)
                        {
                            <label>@item.actualDesignation</label>
                        }
                        else
                        {
                            <label></label>
                        }
                    </td>
                    <td style="text-align: left;" class="col21">
                        @if (item.designationName != null)
                        {
                            <label>@item.designationName</label>
                        }
                        else
                        {
                            <label></label>
                        }
                    </td>
                    <td style="text-align: left;" class="col21">
                        @if (item.memberName != null)
                        {
                            <label>@item.memberName</label>
                        }
                        else
                        {
                            <label></label>
                        }
                    </td>
                    <td style="text-align: left;" class="col21">
                        @if (item.Email != null)
                        {
                            <label>@item.Email</label>
                        }
                        else
                        {
                            <label></label>
                        }
                    </td>
                    <td style="text-align: left;" class="col21">
                        @if (item.Mobile != null)
                        {
                            <label>@item.Mobile</label>
                        }
                        else
                        {
                            <label></label>
                        }
                    </td>
                    <td style="width: 20px; text-align: center;" class="col21 view_hide">
                        @Html.ActionLink("Details", "Details", "GrievanceRedressalCommittee", new { id = item.id }, new { @class = "viewDialog view_button" })
                    </td>
                </tr>
            }
        </table>
        if (ViewBag.CommitteeCount == 0)
        {
            <div class="warning" style="width: 200px; margin: 0 auto;">
                <p>No records found</p>
            </div>
        }

        <div id="dialog-view" style="display: none">
        </div>

        <br />
        <br />

        <script type="text/javascript">
            $(document).ready(function () {

                var ComplaintmainUrl = window.location;
                var courseurl;

                $.ajaxSetup({ cache: false });
                var $myCourseDialog = $("#dialog-edit-course").dialog({
                    title: 'Add/Edit Grievance Redressal Complaints ',
                    autoOpen: false,
                    resizable: false,
                    height: 350,
                    width: 550,
                    show: { effect: 'drop', direction: "up" },
                    modal: true,
                    draggable: true,
                    closeOnEscape: true,
                    open: function (event, ui) {
                        $(this).load(courseurl);
                    },
                    close: function (event, ui) {
                        $(this).dialog("close");
                    }
                });




                $(".viewDialogComplaints").live("click", function (e) {
                    var courseurl = $(this).attr('href');
                    $("#dialog-view-course").dialog({
                        title: 'View Grievance Redressal Complaints ',
                        autoOpen: false,
                        resizable: false,
                        height: 350,
                        width: 550,
                        show: { effect: 'drop', direction: "up" },
                        modal: true,
                        draggable: true,
                        open: function (event, ui) {
                            $(this).load(courseurl);
                        },
                        buttons: {
                            "Close": function () {

                                $(this).dialog("close");

                            }
                        },
                        close: function (event, ui) {
                            $(this).dialog('close');
                        }
                    });

                    $("#dialog-view-course").dialog('open');
                    return false;
                });
                $("#btncancelCourse").live("click", function (e) {
                    $("#dialog-edit-course").dialog("close");
                });

                $($myCourseDialog).bind("dialogclose", function () {
                    window.location = ComplaintmainUrl;
                });
            });
        </script>
        int counter = 0;
        <table class="four_columns" style="width: 100%;">
            <tr>
                <td class="col11" colspan="4">
                    <b>Grievance Redressal Complaints:</b>
                </td>
            </tr>
            <tr style="font-weight: bold;">
                <td style="text-align: center; width: 50px;" class="col11">S.No</td>
                <td style="text-align: left; width: 250px;" class="col11">Complaint</td>
                <td style="text-align: left;" class="col11">Action Taken</td>
                <td style="text-align: left;" class="col11 view_hide"></td>
            </tr>

            @foreach (var item in ViewBag.Complaints)
            {
                <tr>
                    <td style="text-align: center;" class="col21">
                        @(counter = counter + 1)
                    </td>
                    <td style="text-align: left;" class="col21">
                        @if (item.complaintReceived != null)
                        {
                            <label>@item.complaintReceived</label>
                        }
                        else
                        {
                            <label></label>
                        }
                    </td>
                    <td style="text-align: left;" class="col21">
                        @if (item.actionsTaken != null)
                        {
                            <label>@item.actionsTaken</label>
                        }
                        else
                        {
                            <label></label>
                        }
                    </td>
                    <td style="width: 20px; text-align: center;" class="col21 view_hide">
                        @Html.ActionLink("Details", "Details", "GrievanceRedressalComplaints", new { id = item.id }, new { @class = "viewDialog view_button" })
                    </td>
                </tr>
            }
        </table>

        if (ViewBag.ComplaintsCount == 0)
        {
            <div class="warning" style="width: 200px; margin: 0 auto;">
                <p>No records found</p>
            </div>
        }

        <div id="dialog-view-course" style="display: none">
        </div>
    }
</div>
