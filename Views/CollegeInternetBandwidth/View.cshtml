@model IList<UAAAS.Models.InternetBandwidthDetails>
@using UAAAS.Models
@{
    ViewBag.Title = "College Internet Bandwidth";
    Layout = "~/Views/Shared/_LayoutLeftMenu.cshtml";
    string Internetbandwidthcertificate = @ViewBag.InternetbandwidthLetter;
}


@if (ViewBag.IsEditable == true)
{
    <h2>College Internet Bandwidth</h2>
    <p class="create_link_right">
        @Html.ActionLink("Edit", "Edit", "CollegeLibrary", new { @class = "create_button" /* id=Model.PrimaryKey */ })
    </p>
}
else
{
    @*<p class="create_link_right">
        <input name="btnPrint" type="button" onclick="printdiv('div_print');" value=" Print" class="create_button">
    </p>*@
}
<script src="@Url.Content("~/Scripts/jquery.validate.min.js")" type="text/javascript"></script>
<script src="@Url.Content("~/Scripts/jquery.validate.unobtrusive.min.js")" type="text/javascript"></script>
<script type="text/javascript">
    $(document).ready(function () {
        $(".view_hide").show();
    });
    function printdiv(printpage) {
        $(".view_hide").hide();
        //Get the HTML of div
        var divElements = document.getElementById(printpage).innerHTML;
        //Get the HTML of whole page
        var oldPage = document.body.innerHTML;

        //Reset the page's HTML with div's HTML only
        document.body.innerHTML =
          "<html><head><title></title></head><body>" +
          divElements.fontsize(1) + "</body>";

        //Print Page
        window.print();

        //Restore orignal HTML
        document.body.innerHTML = oldPage;
        $(".view_hide").show();
        return false;
    }
</script>

<div id="div_print">
    @if (ViewBag.IsEditable == false)
    {
        <h2>College Internet Bandwidth</h2>
    }
    <table class="webGrid" style="width: 100%;">
        <tr>
            <td colspan="2" class="col1">
                <span>Certificate indicating Internet Bandwidth provided to the college</span>
            </td>
        </tr>
        <tr>
            <td style="text-align: left;" class="col1">
                <span>Upload document:</span>
            </td>
            <td class="col2">
                @*<input type="file" name="fileUploader" id="filepicker" class="file fileUploadcheck" required="true" />*@
                @{
                    //foreach (var item in Model.Take(1).ToList())
                    //{
                    //    AICTEApprovalLettr = item.AICTEApprovalLettr;
                    //}
                }
                @if (!string.IsNullOrEmpty(Internetbandwidthcertificate))
                {
                    <span><a href="../Content/Upload/CollegeEnclosures/@Internetbandwidthcertificate" class="viewFileData" target="_blank"><span style="font-weight: bold; color: red; text-align: center;">View</span></a></span>
                    @*@Html.Hidden("hiddenfile", new { @Value = @AICTEApprovalLettr })*@

                } &nbsp &nbsp &nbsp &nbsp &nbsp
                @*<input type="submit" value="Upload" name="fileUploader" class="subtUpload"/>*@
                @*  @Html.ActionLink("Upload", "FileUpload", "CollegeIntakeExisting", new { collegeId = @ViewBag.collegeId,@name="fileUploader" }, new {enctype = "multipart/form-data", @class = "create_button" })*@
            </td>
        </tr>
    </table>
    <br/>

    @using (Html.BeginForm())
    {

        if (ViewBag.Norecords == true)
        {
        <div class="warning" style="width: 200px; margin: 0 auto;">
            <p>No Data found</p>
        </div>
        }
        else
        {
        <table class="four_columns" style="width: 100%;">
            <tr style="font-weight: bold;">
                <td style="text-align: left;" class="col11">Degree</td>
                @*<td style="text-align: center; width: 140px;" class="col11">Total Intake</td>*@
                <td style="text-align: left;" class="col11">Available Internet Speed (Mbps)</td>
            </tr>

            @for (int i = 0; i < ViewBag.Count; i++)
            {
                <tr>
                    <td style="text-align: left;" class="col21">
                        @Html.LabelFor(m => m[i].degree, Model[i].degree == null ? string.Empty : Model[i].degree.ToString())
                        @Html.HiddenFor(m => m[i].degreeId)
                    </td>
                   @* <td style="text-align: center; width: 15%" class="col21">
                        @Html.LabelFor(m => m[i].totalIntake, Model[i].totalIntake == 0 ? "0" : Model[i].totalIntake.ToString())
                        @Html.HiddenFor(m => m[i].totalIntake)
                    </td>*@
                    <td style="text-align: left;" class="col21 focus">
                        @if (ViewBag.Update == true)
                        {
                            @Html.DisplayFor(m => m[i].availableInternetSpeed, new { Value = Model[i].availableInternetSpeed == 0 ? "0" : Model[i].availableInternetSpeed.ToString(), style = "text-align: center;" })
                        }
                        else
                        {
                            @Html.DisplayFor(m => m[i].availableInternetSpeed, new { Value = Model[i].availableInternetSpeed == 0 ? string.Empty : Model[i].availableInternetSpeed.ToString(), style = "text-align: center;" })
                        }
                    </td>
                </tr>
            }
        </table>
            if (ViewBag.Count == 0)
            {
        <div class="warning" style="width: 550px; margin: 0 auto;">
            <p>Please select your courses in "College Information" page before filling up this page.</p>
        </div>
            }
        }
    }
</div>
