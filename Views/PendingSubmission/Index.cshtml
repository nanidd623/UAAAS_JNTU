@model UAAAS.Models.PendingSubmission
@{
    ViewBag.Title = "Pending Submission Colleges";
}

<h2>Pending Submission Colleges</h2>

@*@section onReady{

/* Initialize table and make first column non-sortable*/
   $('#example').dataTable({  "bJQueryUI": true,
                               "bPaginate": false,
                              "bStateSave": false,
                              "aoColumns": [ { "bSortable": true, "bSearchable": true }, null, null, null ]
                           });
}*@
@using (Html.BeginForm())
{
    int count = 0; 
    
    <div class="details_table"  style="display:none">
        <div class="editor-label" style="width: 10%">
            @Html.LabelFor(model => model.collegeCode)
        </div>
        <div class="editor-field focus" style="width: 15%">
            @Html.TextBoxFor(model => model.collegeCode, new { @class="collegeCode"})
        </div>
        <div class="editor-label" style="width: 10%">
            @Html.LabelFor(model => model.collegeName)
        </div>
        <div class="editor-field " style="width: 54%">
            @Html.TextBoxFor(model => model.collegeName, new { @class="collegeName"})
        </div>
        <input type="submit" value="Search" onclick="return Validate();" style="width:70px;" />
       
    </div>
    
    <br />
    <table id="example" class="display">
        <thead>
            <tr>
                <th style="width: 40px; text-align: center;">S.No
                </th>
                <th style="width: 100px; text-align: center;">College Code
                </th>
                <th style="text-align: left;">College Name
                </th>
                <th class="ui-state-default" style="width: 60px; text-align: center;"></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in ViewBag.Colleges)
            {                
                <tr>
                    <td style="width: 40px; text-align: center;">
                        @(count = count + 1)
                    </td>
                    <td style="width: 100px; text-align: center; text-transform: uppercase;">
                        <label>@item.collegeCode</label>
                    </td>
                    <td style="text-align: left;">
                        <label>@item.collegeName</label>
                    </td>
                    <td style="width: 130px; text-align: center; font-weight: normal;">
                        @Html.ActionLink("View", "View", "CollegeInformation", new { id = UAAAS.Models.Utilities.EncryptString(item.collegeId.ToString(), System.Web.Configuration.WebConfigurationManager.AppSettings["CryptoKey"]) , submissiontype = "PendingSubmission"}, new { @class = "edit_button1", target = "_blank" })
                    </td>
                </tr>
            }
        </tbody>
        <tfoot>
        <tr>
            <th style="width: 40px;">
                <input type="text" id="i-0" style="width: 90%; text-align: left; display: none;" class="search_init">
            </th>
            <th style="width:100px;">
                <input type="text" id="i-1" style="width: 90%; text-align: left;" class="search_init"></th>
            <th>
                <input type="text" id="i-2" style="width: 90%; text-align: left;" class="search_init">         
            <th class=" ui-state-default">
                <input type="text" id="i-3" style="width: 90%; text-align: left;display:none;" class="search_init">
            </th> 
        </tr>
    </tfoot>
    </table>
}

@if (ViewBag.Count == 0)
{
    <div class="warning" style="width: 200px; margin: 0 auto;">
        <p>No records found</p>
    </div>
}

<script type="text/javascript">
    function Validate() {
        var isValid = true;
        var collegeCode = $(".collegeCode").val();
        var collegeName = $(".collegeName").val();
        if (collegeCode == '' && collegeName == '') {
            alert("Please Select either College Code Or College Name");
            isValid = false;
        }
        if (collegeCode != '' && collegeName != '') {
            alert("Please Select either College Code Or College Name");
            isValid = false;
        }
        return isValid;
    }
    var asInitVals = new Array();
    $(document).ready(function () {
        /* Initialize table and make first column non-sortable*/
        var oTable = $('#example').dataTable({
            "bJQueryUI": true,
            "iDisplayLength": 50,
            "bStateSave": false,
            "sDom": '<"top"lfip>rt<"bottom"ip<"clear">',
            //"sPaginationType": "full_numbers",
            "oLanguage": {
                "sSearch": "Search all columns:",
                "oPaginate": {
                    "sFirst": "",
                    "sLast": "",
                    "sNext": "",
                    "sPrevious": ""
                }
            },
            "fnInitComplete": function () {
                var oSettings = $('#example').dataTable().fnSettings();
                for (var i = 0 ; i < oSettings.aoPreSearchCols.length ; i++) {
                    if (oSettings.aoPreSearchCols[i].sSearch.length > 0) {
                        $("tfoot input")[i].value = oSettings.aoPreSearchCols[i].sSearch;
                        $("tfoot input")[i].className = "";
                    }
                }
            },
            "aoColumns": [{ "bSortable": true, "bSearchable": true }, null, null],
            //"aoColumns": [{ type: "select", values: ['Gecko', 'Trident', 'KHTML', 'Misc', 'Presto', 'Webkit', 'Tasman'] },{ type: "text" },null,{ type: "number" },{ type: "select" }]
        });
        /*
         * Support functions to provide a little bit of 'user friendlyness' to the textboxes in
         * the footer
         */
        $("tfoot input").each(function (i) {
            asInitVals[i] = this.value;
        });
        $("tfoot input").focus(function () {
            if (this.className == "search_init") {
                this.className = "";
                this.value = "";
            }
        });
        $("tfoot input").blur(function (i) {
            if (this.value == "") {
                this.className = "search_init";
                this.value = asInitVals[$("tfoot input").index(this)];
            }
        });

        $("tfoot input").keyup(function () {
            /* Filter on the column (the index) of this element */
            oTable.fnFilter(this.value, $("tfoot input").index(this));
        });

    });
</script>
